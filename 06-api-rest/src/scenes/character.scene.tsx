import * as React from 'react';
import { AppLayout } from 'layouts';
import { CharacterContainer } from 'pods/charcter';

export const CharacterScene = () => (
  <AppLayout>
    <CharacterContainer />
  </AppLayout>
);
